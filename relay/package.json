{
  "name": "cove-relay",
  "version": "1.1.1",
  "description": "Optional relay server for Cove remote play (Phase 3 of the seat protocol). A dumb pipe that bridges a DM behind NAT to remote players by room code, and serves the player seat page. Holds no game state.",
  "type": "module",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node server.test.js"
  },
  "dependencies": {
    "ws": "^8.18.0"
  }
}
